// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const initParticipationRequest = z.lazy(() => {
  return z.object({
    eventId: z.any(),
    isHybrid: z.boolean().optional(),
    meetingId: z.string(),
    meetingParticipants: z.array(z.string()),
  });
});

/**
 *
 * @typedef  {InitParticipationRequest} initParticipationRequest
 * @property {any}
 * @property {boolean}
 * @property {string}
 * @property {string[]}
 */
export type InitParticipationRequest = z.infer<typeof initParticipationRequest>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const initParticipationRequestResponse = z.lazy(() => {
  return z
    .object({
      eventId: z.any(),
      isHybrid: z.boolean().optional(),
      meetingId: z.string(),
      meetingParticipants: z.array(z.string()),
    })
    .transform((data) => ({
      eventId: data['eventId'],
      isHybrid: data['isHybrid'],
      meetingId: data['meetingId'],
      meetingParticipants: data['meetingParticipants'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const initParticipationRequestRequest = z.lazy(() => {
  return z
    .object({
      eventId: z.any(),
      isHybrid: z.boolean().optional(),
      meetingId: z.string(),
      meetingParticipants: z.array(z.string()),
    })
    .transform((data) => ({
      eventId: data['eventId'],
      isHybrid: data['isHybrid'],
      meetingId: data['meetingId'],
      meetingParticipants: data['meetingParticipants'],
    }));
});

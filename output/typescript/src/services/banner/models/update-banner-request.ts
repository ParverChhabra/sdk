// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const updateBannerRequest = z.lazy(() => {
  return z.object({
    hidden: z.boolean().optional(),
    type: z.string().optional(),
    bannerVariants: z.array(z.string()).optional(),
  });
});

/**
 *
 * @typedef  {UpdateBannerRequest} updateBannerRequest
 * @property {boolean}
 * @property {string}
 * @property {string[]}
 */
export type UpdateBannerRequest = z.infer<typeof updateBannerRequest>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const updateBannerRequestResponse = z.lazy(() => {
  return z
    .object({
      hidden: z.boolean().optional(),
      type: z.string().optional(),
      bannerVariants: z.array(z.string()).optional(),
    })
    .transform((data) => ({
      hidden: data['hidden'],
      type: data['type'],
      bannerVariants: data['bannerVariants'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const updateBannerRequestRequest = z.lazy(() => {
  return z
    .object({
      hidden: z.boolean().optional(),
      type: z.string().optional(),
      bannerVariants: z.array(z.string()).optional(),
    })
    .transform((data) => ({
      hidden: data['hidden'],
      type: data['type'],
      bannerVariants: data['bannerVariants'],
    }));
});

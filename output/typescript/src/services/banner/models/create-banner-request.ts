// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const createBannerRequest = z.lazy(() => {
  return z.object({
    clientId: z.any(),
    type: z.string(),
    hidden: z.any(),
    bannerVariants: z.array(z.string()),
  });
});

/**
 *
 * @typedef  {CreateBannerRequest} createBannerRequest
 * @property {any}
 * @property {string}
 * @property {any}
 * @property {string[]}
 */
export type CreateBannerRequest = z.infer<typeof createBannerRequest>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const createBannerRequestResponse = z.lazy(() => {
  return z
    .object({
      clientId: z.any(),
      type: z.string(),
      hidden: z.any(),
      bannerVariants: z.array(z.string()),
    })
    .transform((data) => ({
      clientId: data['clientId'],
      type: data['type'],
      hidden: data['hidden'],
      bannerVariants: data['bannerVariants'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const createBannerRequestRequest = z.lazy(() => {
  return z
    .object({
      clientId: z.any(),
      type: z.string(),
      hidden: z.any(),
      bannerVariants: z.array(z.string()),
    })
    .transform((data) => ({
      clientId: data['clientId'],
      type: data['type'],
      hidden: data['hidden'],
      bannerVariants: data['bannerVariants'],
    }));
});

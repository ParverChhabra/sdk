// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import {
  CreateUserRequest,
  createUserRequest,
  createUserRequestRequest,
  createUserRequestResponse,
} from '../../common/create-user-request';

/**
 * The shape of the model inside the application code - what the users use
 */
export const createProgramManagerRequest = z.lazy(() => {
  return z.object({
    userId: z.string().optional(),
    user: createUserRequest.optional(),
    displayImageUrl: z.string().optional(),
    chatLink: z.string(),
  });
});

/**
 *
 * @typedef  {CreateProgramManagerRequest} createProgramManagerRequest
 * @property {string}
 * @property {CreateUserRequest}
 * @property {string}
 * @property {string}
 */
export type CreateProgramManagerRequest = z.infer<typeof createProgramManagerRequest>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const createProgramManagerRequestResponse = z.lazy(() => {
  return z
    .object({
      userId: z.string().optional(),
      user: createUserRequestResponse.optional(),
      displayImageUrl: z.string().optional(),
      chatLink: z.string(),
    })
    .transform((data) => ({
      userId: data['userId'],
      user: data['user'],
      displayImageUrl: data['displayImageUrl'],
      chatLink: data['chatLink'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const createProgramManagerRequestRequest = z.lazy(() => {
  return z
    .object({
      userId: z.string().optional(),
      user: createUserRequestRequest.optional(),
      displayImageUrl: z.string().optional(),
      chatLink: z.string(),
    })
    .transform((data) => ({
      userId: data['userId'],
      user: data['user'],
      displayImageUrl: data['displayImageUrl'],
      chatLink: data['chatLink'],
    }));
});

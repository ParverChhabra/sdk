// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const createQuestionRequest = z.lazy(() => {
  return z.object({
    clientId: z.any(),
    type: z.string(),
    eventLocation: z.string(),
    fieldName: z.string(),
    isRequired: z.boolean(),
    validationMetadata: z.any().optional(),
    variants: z.array(z.string()),
  });
});

/**
 *
 * @typedef  {CreateQuestionRequest} createQuestionRequest
 * @property {any}
 * @property {string}
 * @property {string}
 * @property {string}
 * @property {boolean}
 * @property {any}
 * @property {string[]}
 */
export type CreateQuestionRequest = z.infer<typeof createQuestionRequest>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const createQuestionRequestResponse = z.lazy(() => {
  return z
    .object({
      clientId: z.any(),
      type: z.string(),
      eventLocation: z.string(),
      fieldName: z.string(),
      isRequired: z.boolean(),
      validationMetadata: z.any().optional(),
      variants: z.array(z.string()),
    })
    .transform((data) => ({
      clientId: data['clientId'],
      type: data['type'],
      eventLocation: data['eventLocation'],
      fieldName: data['fieldName'],
      isRequired: data['isRequired'],
      validationMetadata: data['validationMetadata'],
      variants: data['variants'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const createQuestionRequestRequest = z.lazy(() => {
  return z
    .object({
      clientId: z.any(),
      type: z.string(),
      eventLocation: z.string(),
      fieldName: z.string(),
      isRequired: z.boolean(),
      validationMetadata: z.any().optional(),
      variants: z.array(z.string()),
    })
    .transform((data) => ({
      clientId: data['clientId'],
      type: data['type'],
      eventLocation: data['eventLocation'],
      fieldName: data['fieldName'],
      isRequired: data['isRequired'],
      validationMetadata: data['validationMetadata'],
      variants: data['variants'],
    }));
});
